pipeline {
    agent { label 'nodes' }  // Run on node with label 'nodes'

    stages {
        stage('Build') {
            steps {
                echo 'Running on nodes'
            }
        }
    }
}
