pipeline {
    agent { label 'node1' }  // Run on node with label 'node1'

    stages {
        stage('Build') {
            steps {
                echo 'Running on node1'
            }
        }
    }
}
